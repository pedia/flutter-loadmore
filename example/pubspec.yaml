name: LoadMoreExample
description: Example for loadmore plugin.

dependencies:
  flutter:
    sdk: flutter

  flutter_loadmore:
    path: ../

  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
